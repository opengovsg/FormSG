name: Deploy FormSG app to stg-alt

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
    branches:
      - stg-alt

jobs:
  deploy:
    name: Deploy
    uses: ./.github/workflows/deploy-ecs.yml
    with:
      gha-environment: 'stg-alt'
      # Environment configuration
      environment: 'stg-alt'
      environment-site-name: 'stg-alt'
      app-version: ${{ github.sha }}
      app-url: 'https://stg-alt.form.gov.sg'
      react-app-form-sg-sdk-mode: 'staging'
      # AWS configuration
      aws-region: 'ap-southeast-1'
      ecr-repository: 'formsg'
      # ECS configuration
      ecs-cpu: 1024
      ecs-memory: 2048
      ecs-cluster-name: 'formsg-stg-alt-ecs'
      ecs-service-name: 'formsg-stg-alt-ecs-service'
      ecs-task-definition: 'formsg-stg-alt-ecs'
      ecs-task-definition-path: 'deploy/ecs-task-definition.json'
      ecs-container-name: 'formsg-app'
      # CodeDeploy configuration
      codedeploy-application: 'formsg-stg-alt-ecs-app'
      codedeploy-appspec-path: deploy/appspec.yml
      codedeploy-deployment-group: 'formsg-stg-alt-ecs-dg'
      # Datadog config
      dd-env: 'stg-alt'
    secrets:
      # AWS credentials and configuration
      aws-account-id: ${{ secrets.AWS_ACCOUNT_ID }}
      cicd-role: ${{ secrets.IAC_AWS_CI_ROLE_TO_ASSUME }}
      # Monitoring and analytics configuration
      ga-tracking-id: ${{ secrets.GA_TRACKING_ID }}
      dd-rum-app-id: ${{ secrets.DD_RUM_APP_ID }}
      dd-rum-client-token: ${{ secrets.DD_RUM_CLIENT_TOKEN }}
      dd-sample-rate: ${{ secrets.DD_SAMPLE_RATE }}
      DD_API_KEY: ${{ secrets.DD_API_KEY }}
