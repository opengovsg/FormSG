version: 1
Resources:
  - TargetService:
      Type: 'AWS::ECS::Service'
      Properties:
        # Placeholder text - gets replaced when pipeline runs
        TaskDefinition: '<TASK_DEFINITION>'
        LoadBalancerInfo:
          ContainerName: formsg-app
          ContainerPort: 8080
