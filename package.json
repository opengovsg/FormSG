{
  "name": "formsg-monorepo",
  "version": "6.312.0",
  "private": true,
  "description": "Form Manager for Government",
  "homepage": "https://form.gov.sg",
  "authors": [
    "FormSG <formsg@open.gov.sg>"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/opengovsg/formsg.git"
  },
  "engines": {
    "node": ">=22.22",
    "pnpm": ">=10.30.3"
  },
  "scriptComments": {
    "clean": "remove all current build artifacts"
  },
  "scripts": {
    "test": "pnpm test:backend && pnpm run test:frontend",
    "test:backend": "pnpm --filter formsg-backend test",
    "test:backend:ci": "pnpm --filter formsg-backend test:ci",
    "test:backend:watch": "pnpm --filter formsg-backend test:watch",
    "test:frontend": "pnpm --prefix frontend test",
    "test:e2e-v2": "pnpm build && npx playwright test",
    "test:e2e-v2:server": "env-cmd -f ./apps/backend/__tests__/setup/.test-env pnpm test-e2e-server",
    "test-e2e-server": "concurrently --success last --kill-others \"mockpass\" \"maildev\" \"node dist/backend/src/app/server.js\" \"node ./apps/backend/__tests__/setup/mock-webhook-server.js\"",
    "build": "pnpm clean && concurrently \"pnpm:build:*\"",
    "build:frontend": "pnpm --prefix frontend build",
    "build:backend": "pnpm --filter formsg-backend build",
    "clean": "rimraf dist/",
    "start": "pnpm --filter formsg-backend start",
    "dev": "concurrently -k -p \"[{name}]\" -n \"api,client,virus-scanner-guardduty,pdf-gen\" -c  \"yellow.bold,green.bold,cyan.bold,magenta.bold\" \"docker-compose up\" \"pnpm dev:frontend\" \"pnpm --prefix services/virus-scanner-guardduty run dev\" \"pnpm --prefix services/pdf-gen-sparticuz run dev\"",
    "dev:backend": "pnpm --filter formsg-backend dev",
    "dev:virus-scanner-guardduty": "pnpm --prefix services/virus-scanner-guardduty run dev",
    "dev:pdf-gen": "pnpm --prefix services/pdf-gen-sparticuz run dev",
    "dev:frontend": "pnpm --prefix frontend start",
    "lint": "pnpm --filter formsg-backend lint && eslint packages/shared/ --quiet --fix",
    "lint:shared": "eslint packages/shared/ --quiet --fix",
    "lint:frontend": "pnpm --prefix frontend lint",
    "lint-ci": "pnpm --filter formsg-backend lint-ci && eslint packages/shared/ --quiet",
    "version": "auto-changelog -p && git add CHANGELOG.md",
    "prepare": "husky",
    "pre-commit": "lint-staged",
    "storybook": "pnpm --prefix frontend storybook"
  },
  "dependencies": {
    "formsg-backend": "workspace:*",
    "formsg-shared": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "auto-changelog": "^2.5.0",
    "concurrently": "^7.6.0",
    "env-cmd": "^10.1.0",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^8.10.0",
    "eslint-plugin-prettier": "^5.2.1",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "maildev": "^2.2.1",
    "prettier": "^3.5.3",
    "rimraf": "^5.0.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "@datadog/native-appsec",
      "@datadog/native-iast-taint-tracking",
      "@datadog/native-metrics",
      "@datadog/pprof",
      "aws-sdk",
      "aws-lambda-ric",
      "canvas",
      "dd-trace",
      "esbuild",
      "fsevents",
      "protobufjs",
      "sharp",
      "unix-dgram",
      "msw",
      "microtime"
    ]
  },
  "config": {
    "//": [
      "This is required for the mongodb binary to be downloaded on M1 macs",
      "to run the test suite, since mongo does not have an arm image."
    ],
    "mongodbMemoryServer": {
      "arch": "x64"
    }
  }
}
