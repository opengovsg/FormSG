/**
 * Maximum number of results to return per page.
 */
export const PAGE_SIZE = 16

/**
 * Minimum number of submissions before example form is returned from search.
 */
export const MIN_SUB_COUNT = 10
