// Main tsconfig for building, prevents tests from being compiled.
{
  "extends": "./tsconfig.json",
  "exclude": [
    "./dist",
    "**/*.test.ts",
    "**/__tests__/",
    "**/__mocks__/",
    "../shared/**/__tests__",
    "../shared/**/*.spec.ts",
    "../shared/**/*.test.ts"
  ]
}
